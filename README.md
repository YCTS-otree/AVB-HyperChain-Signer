# AVB-HyperChain-Signer
A zero-guess, full-chain Android Verified Boot signing engine that automatically analyzes, deduplicates, reconstructs and resigns AVB chains — including parent vbmeta aggregation for Algorithm NONE partitions — using raw partition dumps.
